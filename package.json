{
  "name": "local-reader-extension",
  "version": "2.2.0",
  "private": true,
  "description": "Build-time dependencies for the Local Reader Chrome extension.",
  "scripts": {
    "vendor": "node scripts/vendor-dependencies.mjs",
    "check": "node --check background.js && node --check reader.js && node --check tests/chrome-smoke-test.mjs"
  },
  "devDependencies": {
    "@mozilla/readability": "0.6.0",
    "dompurify": "3.4.13"
  }
}
